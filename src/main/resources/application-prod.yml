mp:
  jwt:
    verify:
      publickey:
        location: classpath:jwt/publicKey.pem
      algorithm: RS256
    issuer: Dompetly App

smallrye:
  jwt:
    sign:
      key:
        location: classpath:jwt/privateKey.pem
      key-format: PEM
      algorithm: RS256
